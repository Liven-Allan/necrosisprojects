version: '3.8'
services:
  backend:
    build: ./NecrosisApi
    volumes:
      - ./NecrosisApi/media:/app/media
    ports:
      - "8000:8000"
    env_file:
      - ./NecrosisApi/.env
    depends_on:
      - frontend

  frontend:
    build: ./necrosisapp
    ports:
      - "3000:80"